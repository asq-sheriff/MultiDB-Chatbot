# MultiDB Chatbot Environment Configuration Template
# Copy this file to .env and update the values

# ================================================================
# DATABASE CONFIGURATION
# ================================================================

# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=chatbot_app
POSTGRES_USER=chatbot_user
POSTGRES_PASSWORD=your_secure_password_here_change_me

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ScyllaDB Configuration
SCYLLA_HOST=localhost
SCYLLA_PORT=9042
SCYLLADB_HOST=localhost
SCYLLADB_PORT=9042

# ================================================================
# APPLICATION CONFIGURATION
# ================================================================

# Security (CRITICAL: Change this!)
SECRET_KEY=your-super-secret-jwt-key-change-this-immediately

# Application Settings
LOG_LEVEL=INFO
API_RATE_LIMIT=100
ENABLE_POSTGRESQL=true

# Feature Flags
ENABLE_CACHING=true
ENABLE_ANALYTICS=true
ENABLE_NOTIFICATIONS=true

# ================================================================
# DEVELOPMENT CONFIGURATION
# ================================================================

# Environment
NODE_ENV=development
APP_PORT=8000

# Debug Settings (set to false in production)
DEBUG=true
RELOAD=true

# ================================================================
# SECURITY NOTES
# ================================================================

# 🔐 IMPORTANT SECURITY REMINDERS:
# 1. Never commit the actual .env file to git
# 2. Always change default passwords in production
# 3. Generate a strong SECRET_KEY for JWT tokens
# 4. Use environment-specific configurations
# 5. Review all settings before deploying

# 🎯 QUICK SETUP:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Never commit .env to git (it's in .gitignore)